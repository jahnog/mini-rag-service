# Copy to deploy/local.env (gitignored).
# DEPLOY_HOST=user@your-dump-host
# DEPLOY_USER=your-service-user   # required if DEPLOY_HOST has no @
# DEPLOY_DIR=/path/on/the-dump-host   # default: remote $HOME/bcra-mini-rag
