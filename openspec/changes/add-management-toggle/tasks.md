## 1. assistant-ui

- [x] 1.1 Add `title_markdown`, `LAYOUT_STAFF` (`Staff (IA)`), `LAYOUT_USER` (`Usuario`), `LAYOUT_HELP`, and `layout_updates(staff: bool)` in `src/bcra_rag/ui/config.py`. Split the topbar into always-visible title + `#layout-toggle` Vista radio (default `Staff (IA)`) + `#layout-toggle-help` + `#observatory-freeze` (`banner_markdown`). Wire radio `.change` to hide/show freeze chips and `#observatory-side` only (do not clear chat or session). CSS for `#layout-toggle` / `#layout-toggle-help`. Keep `topbar_markdown` freeze-field contract. Verify `uv run pytest tests/test_ui.py -q`: default `#observatory-side` and `#observatory-freeze` are visible; `#layout-toggle` and `#layout-toggle-help` exist; help text and both choice strings are on the tree; `layout_updates(False)` hides freeze + side and `True` shows them; `title_markdown` has “no oficial” and does not require freeze chips; `banner_markdown` / `topbar_markdown` still contain `to_as_of`, `last_refresh`, last A, n_docs
- [x] 1.2 Keep existing `test_ui.py` contracts: observatory `elem_id`s, canned prompts, L1 collapsed, copy-id, trust, abstain, no `gr.Examples`, JSON inspector/trust `visible=False`, Enviar primary / Clear secondary, `build_blocks` does not call the LLM or L1. Verify `uv run pytest tests/test_ui.py -q`
- [x] 1.3 Run `uv run pytest tests/test_ui.py -q` then `uv run pytest -q` and `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. No README `## How to run` edit (no command added)
