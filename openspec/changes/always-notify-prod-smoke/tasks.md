## 1. prod-smoke

- [x] 1.1 Rename `scripts/notify_prod_smoke_failure.py` to `scripts/notify_prod_smoke.py`. Subject from exit code: `0` → `BCRA Mini-RAG production smoke OK`, `124`/`137` → `TIMED OUT`, else `FAILED`. Keep `--exit-code` / `--duration` / `--log`, recipient `PROD_SMOKE_NOTIFY_TO` else `LIVE_EMAIL`, body `exit=` / `duration_s=` / `timed_out=` plus last 80 log lines. Verify `uv run pytest tests/test_prod_smoke_cron.py -q` after extending fake-SMTP tests for success, failure, and timeout, and that tracked files still do not name the mailbox or public host
- [x] 1.2 In `scripts/run-prod-smoke-cron.sh`, drop the `CODE -eq 0` early exit. After appending the run log, always call `notify_prod_smoke.py`, then `exit "$CODE"`. Flock miss still logs skip and exits 0 with no mail. Update `scripts/prod-smoke.crontab` comment: emails on success and on failure; keep `MAILTO=` empty. Verify `uv run pytest tests/test_prod_smoke_cron.py -q`: wrapper names the renamed helper, still has flock and `15m`, and does not exit 0 before notify after a completed run
- [x] 1.3 Update README.md `## How to run` production-smoke bullet, `AGENTS.md`, and `.env.example` on `PROD_SMOKE_NOTIFY_TO`: the daily wrapper emails on success and on failure (OK / FAILED / TIMED OUT) via `AUTH_SMTP_*`. No new command fence; TUI catalog unchanged. Verify `uv run pytest tests/test_notes.py tests/test_no_private_layout.py tests/test_prod_smoke_cron.py -q`
- [x] 1.4 Run `uv run ruff check .`, `uv run mypy src`, then `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. Do not run a paid L1 eval. Do not run `--run-prod-smoke` or `--run-live-server` as a CI-blocking task
