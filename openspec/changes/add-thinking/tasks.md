## 1. query-answering

- [x] 1.1 Add optional `thinking` to `LlmDraft` (default `""`) and `ChatResponse` (default `None`). Keep `ChatRequest` `extra=forbid` so a request body with `thinking` is rejected. `FakeLlm` may set `thinking`. Verify `uv run pytest tests/test_schemas.py tests/test_llm_port.py -q`: request with `thinking` raises; response without thinking still constructs; FakeLlm round-trip keeps a trace
- [x] 1.2 In `LlmAdapter.complete`, extract thinking from `reasoning_content`, then string `reasoning`, then `<think>` / `<thinking>` tags stripped from content; parse remaining content as JSON (`answer` / `finding` / `citations` only). Send `extra_body` `enable_thinking` + `chat_template_kwargs.enable_thinking` only when `Settings.llm_enable_thinking` is not `None`. Verify `uv run pytest tests/test_llm_port.py -q`: xAI-shaped stub; llama.cpp-shaped stub (`reasoning_content` + JSON content); think-tags in content; extra_body omitted when unset and present for true/false
- [x] 1.3 Copy `draft.thinking` onto `ChatResponse` only after a successful generate. Leave thinking unset on `/clear`, blocking guardrails, empty retrieval, `index_not_ready`, and `llm_unavailable`. `_remember` still stores the answer, not the trace. Verify `uv run pytest tests/test_answer_query.py tests/test_chat_api.py -q`: in-corpus with FakeLlm thinking; named Com. A; silencio / clear / missing key have no thinking; follow-up still cites the dump
- [x] 1.4 Stream Chat Completions inside `LlmAdapter.complete` (`stream=True`). Optional `on_thinking` on `LlmPort.complete` / FakeLlm / UnavailableLlm. Incremental extras then think-tags (tags may split across chunks); JSON body never goes to the callback or into `thinking` (strip a copied JSON object). `AnswerQuery` / `handle_turn` pass the callback through. Verify `uv run pytest tests/test_llm_port.py tests/test_answer_query.py -q`: reasoning then JSON chunks grow `on_thinking`; split `<think>` tags; JSON-only stream leaves thinking empty; FakeLlm `think_chunks` fires the callback; HTTP still one `ChatResponse`

## 2. query-logging

- [x] 2.1 Strip `thinking` in `_log_turn` so console and `chat.log` omit the trace while still logging message, answer, finding, citation ids, and the guardrail log. Verify `uv run pytest tests/test_logconfig.py -q` (and any chat-turn log assertion): in-corpus with a FakeLlm trace does not persist `thinking`; `/clear` and empty-hits still log without a trace

## 3. assistant-ui

- [x] 3.1 Turn `_turn` into an async generator: first yield user + pending thought (`Pensando…`, `status=pending`) with `gr.skip()` on inspector/trust/abstain; after `handle_turn`, drop pending; if `response.thinking` append done thought (`Pensó Ns`) then the answer bubble, else answer (or 401/429 notice) only. Extend `append_messages` for optional thinking metadata. Verify `uv run pytest tests/test_ui.py -q`: pending then done message shape; no leftover pending on silencio; 401/429 replace pending; both layouts keep the thought in the chat stage
- [x] 3.2 Style `#observatory-chat .thought-group` (muted, distinct from the answer bubble, pending pulse). Do not restyle `.assistant.message` / `.bot.message`. Do not hardcode hashed `svelte-*` classes. Status tracker stays hidden. Verify `uv run pytest tests/test_ui.py::test_observatory_css_tokens -q` asserts `.thought-group` and a pending/pulse rule, and existing observatory tokens still pass
- [x] 3.3 Extract the observatory turn generator. Latest-wins Event yields pending thought content as `on_thinking` fires, before any answer row. `group_consecutive_messages=False`. Latest completed thought omits `status` (open, collapsible); priors `status=done` on copied metadata. CSS: mute `.bot:has(.thought-group)`, cap `.thought-group .content`. Cancel the turn task on generator exit. Verify `uv run pytest tests/test_ui.py -q`: growing pending content before the answer; no JSON/`Fuente:` in the thought; no leftover thought on silencio/401/429; Chatbot flag; CSS tokens

## 4. settings and README

- [x] 4.1 Add `llm_enable_thinking: bool | None = None`. Comment it unset in `.env.example` and `deploy/env.remote.example`. README `## How to run` Debug: one line that local Qwen3.6-35B-A3B uses existing `LLM_*`, thinking is a provider trace, and `LLM_ENABLE_THINKING` plus a longer `LLM_TIMEOUT_S` are optional. No new command fence. Verify `uv run pytest tests/test_settings.py -q` and that README Debug names those vars
- [x] 4.2 Run `uv run pytest -q` then `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. Do not run a paid L1 eval
