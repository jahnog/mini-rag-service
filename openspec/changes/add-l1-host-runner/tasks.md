## 1. platform

- [x] 1.1 Add `deploy/l1.sh` in the ingest/refresh shape: `#!/bin/bash`, `set -euo pipefail`, `exec 9>/run/bcra-rag-job.lock`, `flock 9` (blocking, not `-n`), `trap 'systemctl start bcra-rag.service || true' EXIT`, `systemctl stop bcra-rag.service || true`, `cd __DEPLOY_DIR__`, `sudo -u __DEPLOY_USER__ __DEPLOY_DIR__/.venv/bin/python evals/run_l1.py "$@"`. No cron, no systemd unit, no HTTP, no `uv sync`. Verify `uv run pytest tests/test_deploy.py -q` after extending tests: shebang, `exec 9>`, flock before trap, trap before stop, cd before python, absolute venv, `"$@"`, placeholders, no `Conflicts=`, no `[Install]`. Do not run a paid L1 eval
- [x] 1.2 In `scripts/deploy.sh`, rsync-exclude `evals/l1.json` (keep `--delete-excluded` off). After the tree rsync, if dest `evals/l1.json` is missing, copy local `evals/l1.json` only when that file is unpublished or sample; if local is published, leave dest missing. Render `__DEPLOY_DIR__` / `__DEPLOY_USER__` on `deploy/l1.sh` with the other helpers. Verify deploy tests: exclude list includes `evals/l1.json`, seed copies only unpublished/sample, no `--delete-excluded`, `l1.sh` in the sed render list, still no lab hostname
- [x] 1.3 Add commented `JUDGE_MODEL`, `JUDGE_BASE_URL`, `JUDGE_API_KEY`, `JUDGE_REASONING_EFFORT` to `deploy/env.remote.example` (no `export`). Do not overwrite an existing remote `.env`. Verify `test_remote_env_seed_uses_loopback_embeddings_and_grok` (or sibling) still forbids `export` and pins Grok embeddings; new test that those `JUDGE_` keys appear as comments or empty values
- [x] 1.4 Add `scripts/run-l1.sh`: same `DEPLOY_HOST` / `deploy/local.env` resolution as `deploy.sh`; SSH `sudo` the rendered helper; forward remaining argv; usage on missing host. Verify a unit test reads the script: requires `DEPLOY_HOST`, sources `deploy/local.env`, mentions `deploy/l1.sh`, does not embed a lab hostname, does not `shell=True`

## 2. evals-l1

- [x] 2.1 Keep `evals/run_l1.py` as the only Python entry. Do not add a second scorer or HTTP route. Verify `uv run pytest tests/evals/test_isolation.py -q`: operator script still `build_evals` not `build_app`; chat/jobs sources still do not import `bcra_rag.evals`; no `/evals` route; refresh/ingest still do not run L1
- [x] 2.2 Do not add `uv sync` to `deploy/l1.sh`. Document skip_reason `missing_extra` in README (task 3.1). Verify the helper source contains no `$HOME/.local/bin/uv` and no `uv sync`. Do not run a paid L1 eval

## 3. assistant-ui

- [x] 3.1 Update README.md `## How to run`: `### Evals` distinguishes laptop vs dump host; laptop without ingest is unpublished/sample; dump host uses `./scripts/run-l1.sh` (placeholder `user@dump-host`); serving is down for the run and in-process sessions do not survive; the API starts again on L1 failure so Calidad L1 reloads the stored document (banner follows unpublished/sample); judged extra optional and not installed by the helper; gold / `evals/l1.json` / `l1.log` unchanged. `### Deploy` / systemctl fence: no cron L1; helper is on-demand. Verify `uv run pytest tests/test_notes.py -q` after extending `test_readme_operator_bullets` for the dump-host command, unpublished without ingest, serving down, API start on failure, and banner-follows-file
- [x] 3.2 Add a `scripts/commands.toml` row for the laptop wrapper (`confirm = true`, `readme_line` with `user@dump-host`). Keep the four local `evals` rows. Add the wrapper argv to `ALLOWED_RUNNABLE` in `scripts/devtui.py`. Run `uv run python scripts/devtui.py --sync`. Verify `uv run pytest tests/test_devtui.py -q` (`--check` green, new id in catalog allowlist, no `sudo` executed by the picker)
- [x] 3.3 Run `uv run ruff check .`, `uv run mypy src`, `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml` and fix until green with src coverage >= 80%. Do not run a paid L1 eval
