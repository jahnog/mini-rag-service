## 1. query-logging

- [x] 1.1 In `AnswerQuery.run`, wrap the current body as `_respond` (leave `_remember` on the same paths) and emit `chat_turn` with `message`, `k`, `filters`, and `response.model_dump()`. Verify `uv run pytest tests/test_answer_query.py -q`: with `configure_logging` against a temp file, an in-corpus turn, a no-advice block (“Debería comprar dólares?”), `/clear`, a named A 3500 question, and an empty-retrieval silencio each write JSON `event=chat_turn` to stdout and the file; in-corpus includes the question, answer, a citation dump id, and all five v1 rules; A 3500 includes citation id `A3500`; the block has `no-advice` `block` and does not call the fake LLM; `/clear` has empty citations and does not call the LLM; silencio has finding `silencio` and empty citations
- [x] 1.2 In `app.py`, call `configure_logging(log_file=Settings().data_dir / "logs" / "chat.log")` before `get_app()`. Rewrite `tests/test_logconfig.py::test_app_configures_logging_without_file` so the API source passes `chat.log` and does not mention `ingest.log`. Verify `uv run pytest tests/test_logconfig.py -q` still proves ingest/refresh write `ingest.log` and omitting `log_file` creates no file
- [x] 1.3 After `run_l1` writes the static results document, emit `l1_run` with the published payload. In `evals/run_l1.py`, after loading Settings, call `configure_logging(log_file=settings.data_dir / "logs" / "l1.log")`. Verify `uv run pytest tests/test_l1.py -q`: a fake-index run with `configure_logging` against a temp file writes JSON `event=l1_run` including `headline_metric=citation_id_exact`, hit@5, MRR, slices, unpublished/sample, and `n`; a second run still contains the first line; the operator script source mentions `l1.log` and does not boot Gradio. Do not run a paid L1 eval
- [x] 1.4 Update README.md `## How to run` Debug so ingest stays on `DATA_DIR/logs/ingest.log`, chat turns append to `DATA_DIR/logs/chat.log`, and an L1 operator run still overwrites `evals/l1.json` and appends the same metrics to `DATA_DIR/logs/l1.log`. Verify `uv run pytest tests/test_notes.py::test_readme_operator_bullets -q` after extending that test for `chat.log` and `l1.log`
- [x] 1.5 Run `uv run pytest -q` and `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml` and fix until green with src coverage >= 80%
