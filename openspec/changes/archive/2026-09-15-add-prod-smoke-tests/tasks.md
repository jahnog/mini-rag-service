## 1. platform

- [x] 1.1 Extend `tests/conftest.py`: `--run-prod-smoke`; marker `prod_smoke` registered in `pyproject.toml` next to `live_server`. Third independent skip loop in `pytest_collection_modifyitems` — never return early. Without `--run-prod-smoke`, skip `prod_smoke`. Live and integration flags MUST NOT unskip it. With `--run-prod-smoke`, fail (do not spawn uvicorn) if `PROD_BASE_URL` is missing or unreachable, if `AUTH_PUBLIC_ORIGIN` does not origin-match `PROD_BASE_URL`, if `LIVE_IMAP_*` is missing, or if `PHOENIX_COLLECTOR_ENDPOINT` / `PROD_PHOENIX_PROJECT_NAME` is missing. Do not fall back to `PHOENIX_PROJECT_NAME`. Verify default `uv run pytest tests/test_ci_workflow.py tests/test_notes.py -q` still passes and `.github/workflows/test.yml` is unchanged
- [x] 1.2 Add `PROD_BASE_URL` and `PROD_PHOENIX_PROJECT_NAME` to `.env.example` (empty values). Notes: pytest-process `AUTH_PUBLIC_ORIGIN` must origin-match `PROD_BASE_URL`; reuse `LIVE_IMAP_*` / `LIVE_EMAIL` and `PHOENIX_COLLECTOR_ENDPOINT` / `PHOENIX_API_KEY`; do not name the public hostname or mailbox. Document in README `## How to run` a new Test command fence `test-prod-smoke` (`uv run pytest --run-prod-smoke -m prod_smoke -q`). Add the same argv to `AGENTS.md`, `scripts/commands.toml` (`confirm = true`, keep `block`/`id` aligned with the README fence), and `scripts/devtui.py` `ALLOWED_RUNNABLE`. Verify `uv run pytest tests/test_notes.py tests/test_no_private_layout.py tests/test_ci_workflow.py tests/test_devtui.py -q`, `uv run python scripts/devtui.py --check`, and that README lists the new command

## 2. prod-smoke

- [x] 2.1 Extend `tests/features/live/mailbox.py` with `parse_login_url` (plaintext `/auth/link/[A-Za-z0-9_-]+`; must not confuse the token with the 6-digit code). Unit-test parse, “subject must not contain the token”, and “new UID only — leftover UNSEEN is not success” in `tests/test_live_mailbox.py` with `FakeMailbox` (no network). Verify `uv run pytest tests/test_live_mailbox.py -q`
- [x] 2.2 Add a prod HTTP helper (httpx against `PROD_BASE_URL`, `Origin`/`Referer` from `AUTH_PUBLIC_ORIGIN`, `x-demo-key` when set). Wash `GET /auth/link/{token}` MUST use `follow_redirects=False`. Confirm is `POST /auth/link` with matching Origin (no `Sec-Fetch-Mode: navigate`) on a **cookie-less** client that never called `/auth/request`. HTTP 429 is fail-fast. Unit-test origin-match error and wash-no-follow with `respx` in `tests/test_prod_http_client.py`. Verify `uv run pytest tests/test_prod_http_client.py tests/test_live_http_client.py -q`; default suite still has no live network
- [x] 2.3 Add a collector poll helper: strip `/v1/traces` like `_collector_host`; `GET {host}/v1/projects/{PROD_PHOENIX_PROJECT_NAME}/spans`; Bearer token when `PHOENIX_API_KEY` is a real key (not `${…}`); poll ~1s, bound ~90s; match `chat.turn` `input.value` substring then require `retrieve` / `scope` children by `trace_id`. Do not import `phoenix.client`. Unit-test with `respx` in `tests/test_prod_phoenix.py` (match, timeout fail, missing project/key, ignore unexpanded placeholder, host strip). Verify `uv run pytest tests/test_prod_phoenix.py tests/evals/test_sink.py -q`
- [x] 2.4 Add **one** module `tests/prod/test_smoke.py` marked `prod_smoke`. Session-scoped fixtures encode the DAG (later depend on earlier; tests only assert, they do not re-send mail): `prod_health` (dump fields, `index_ready` true or fail); `prod_otp_session` (new-mail OTP on client A → `/auth/me`, HttpOnly, Secure when HTTPS); `prod_chats` (A 3500 citation id `A3500` or fail on any silencio including `llm_unavailable` / `missing_document`; weather → finding `silencio` and scope `block`); `prod_traces` (collector poll as in 2.3, overlaps the send gap); `prod_link_session` (client A requests send 2; **cookie-less client B** wash+confirm; typed code no longer authenticates). No leftover UNSEEN; no Chromium; do not split across files whose collection order is the DAG. Verify default `uv run pytest -q` skips `prod_smoke` without network
- [x] 2.5 Run `uv run ruff check .`, `uv run mypy src`, then `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. Do not run a paid L1 eval. Do not run `--run-prod-smoke` or `--run-live-server` as a CI-blocking task
