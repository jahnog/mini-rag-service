## 1. Package skeleton

- [x] 1.1 `uv init` package `bcra_rag` under `src/bcra_rag` with `pyproject.toml` (pydantic v2, pydantic-settings, chromadb, openai, httpx, structlog, pytest, pytest-asyncio, pytest-cov, respx, ruff, mypy) and verify `uv sync` and `uv run ruff check .` succeed
- [x] 1.2 Add `Settings`, structlog JSON, `.env.example` (`EMBEDDING_*`, `DATA_DIR`, download concurrency/delay, User-Agent) and verify mypy on Settings plus a unit test that extra env keys do not break load
- [x] 1.3 Create ports (`CatalogPort`, `ExtractorPort`, `IndexPort`, `LlmPort`, `SessionStore`), empty adapters for the first three, Protocol stubs only for `LlmPort` and `SessionStore`, `composition.build_ingest` wiring Settings, and verify a unit test that the ingest composition exposes catalog, extractor, and index
- [x] 1.4 Dockerfile + compose **job** services for ingest and refresh (poppler, volume for `data/bcra/current` and `data/index`, `restart: "no"`, profile or equivalent so default `docker compose up` does not start either job) and verify `docker compose config` is valid and the default project does not include refresh
- [x] 1.5 GitHub Actions `test.yml` (ruff, mypy, pytest) that does not call live BCRA or a live embedding API, and verify the workflow file exists and has no `bcra.gob.ar` URL in job steps

## 2. Corpus ingest

- [x] 2.1 BCRA catalog adapter (tipo A, circular CAMEX, newest-first pagination, unique id / duplicate rows collapse to one stored document) with respx tests that no non-bcra host is requested, duplicate catalog ids become one MANIFEST key, A 13 is stored, and the newest fixture id is stored
- [x] 2.2 PDF download with User-Agent, semaphore 2–4, ~200ms gap, zero-padded URLs, GET-first-bytes/content-type (do not trust HEAD), UTF-8 then latin-1/cp1252 extract, hyphen-join and running-header strip; verify unit tests on strip/hyphen/encoding, that a non-PDF body is not stored, and a fake-clock/interceptor test that simultaneous BCRA requests stay within Settings concurrency
- [x] 2.3 Classifier: TO reprint pack → short event (wins if title is also Adecuación); other CAMEX A including Adecuaciones → full extract; post-`to_as_of` A’s stay full text; TO `t-excbio.pdf` → full structure-aware extract + `to_as_of` from header; verify unit tests for each title class
- [x] 2.4 `FixedChunker` (~512/128) and `StructuredChunker` (sección/punto/anexo, merge <80 tokens, heading_path); verify unit tests on TO-like numbered text and tiny-punto merge
- [x] 2.5 Chroma `IndexPort` with metadata (`doc_kind`, `fecha`, `numero`, `punto`, `chunker`, `doc_part`), stable ids `{doc_id}:{punto|digest}`, serving B on TO + clean A’s and A on the rest; upsert event records; skip upsert only when dump checksum and index both have the id; re-upsert from extract when the index is missing; on TO checksum change delete existing TO chunks for that `doc_id` then upsert; verify an in-memory Index fake, `chunker` metadata, event-record upsert, missing-index repair without dump wipe, and that a punto only in the old TO is absent after replace
- [x] 2.6 `IngestCorpus` full mode + `python -m bcra_rag.jobs.ingest`: empty dump stores catalog + TO, sets `last_refresh` / `to_as_of` / `last_comm_id`, upserts the index (full extracts and event records), includes A 13; checkpoint only after extract and index write; interrupted dump resumes and does not wipe; after `last_refresh` is set a re-run does not fetch A 8465; verify tests for sha256 skip when dump and index match, remaining ids continue, resumed first run sets `last_refresh`, complete re-run does not store A 8465, and the fake index contains stored docs and event records
- [x] 2.7 `python -m bcra_rag.jobs.refresh`: refuse with non-zero exit and operator message when MANIFEST is missing, empty/placeholder, or `last_refresh` is unset (no catalog crawl); append unknown ids; rebuild TO only on checksum change and then set `to_as_of` from the new header; set `last_refresh` and `last_comm_id`; upsert only changed documents; verify refresh idempotency, “new A 8465 after A 8464”, unchanged TO not rebuilt, changed TO updates `to_as_of` and drops old-only puntos, and the three refuse cases
- [x] 2.8 Write operator dump note `data/bcra/current/NOTES.md` for the 1990–97 hole (not a crawl) and README notes: poppler for non-Docker `python -m` runs, `EMBEDDING_*` required for a real upsert, ingest once on the volume host, schedule refresh there (not GHA-hosted Chroma), wipe by deleting the volume, second ingest after success does not pull new catalog ids; verify NOTES mentions the hole, a test that untagged 1990–97 A ids are not fetched solely to close the jump, and README contains those operator bullets
