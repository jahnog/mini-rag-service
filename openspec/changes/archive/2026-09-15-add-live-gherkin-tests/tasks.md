## 1. platform

- [x] 1.1 Extend `tests/conftest.py`: `--run-live-server`; markers `live_server`, `live_http`, `live_ui` registered in `pyproject.toml` next to `integration` / `bdd`. Two independent skip loops in `pytest_collection_modifyitems` — never return early. Without `--run-live-server`, skip `live_server`. Without `--run-integration`, skip `integration`. If both flags are passed, both suites run. Every live `.feature` MUST have Feature-level tags `@live_server` plus `@live_http` or `@live_ui`. With `--run-live-server`, fail if `LIVE_BASE_URL` (default `http://127.0.0.1:8000`) is unreachable — do not spawn uvicorn. Verify `uv run pytest tests/conftest.py tests/test_notes.py -q` still passes existing integration-flag docs; default `uv run pytest -q` does not collect live failures or duplicate-step errors
- [x] 1.2 Add `LIVE_*` keys to `.env.example` (`LIVE_BASE_URL`, `LIVE_IMAP_HOST`, `LIVE_IMAP_PORT`, `LIVE_IMAP_USER`, `LIVE_IMAP_PASSWORD`, `LIVE_IMAP_MAILBOX`, `LIVE_IMAP_SSL`, `LIVE_EMAIL`). Notes: `LIVE_BASE_URL` must origin-match `AUTH_PUBLIC_ORIGIN`; live suite is HTTP-local (non-Secure cookies); `DEMO_API_KEY` unset or sent as `x-demo-key`; `LIVE_IMAP_SSL=false` is plain IMAP (set PORT, typically 143), not STARTTLS. Do not add them to `AuthSettings`. Document in README `## How to run` a new Test command fence `test-live-server` (`uv run pytest --run-live-server -m live_server -q`) plus `playwright install chromium` under Setup. Add `scripts/commands.toml` entry (`confirm = true`) and the same argv tuple to `scripts/devtui.py` `ALLOWED_RUNNABLE`. Keep `block`/`id` aligned with the README fence. Mention local llama.cpp Qwen3.6-35B-A3B or xAI grok-4.3 via existing `LLM_*`; do not write private hostnames. Verify `uv run pytest tests/test_notes.py tests/test_no_private_layout.py tests/test_ci_workflow.py tests/test_devtui.py -q`, `uv run python scripts/devtui.py --check`, and that README lists the new command
- [x] 1.3 Add `playwright` to the uv **dev** group. Default pytest and CI MUST NOT require Chromium or IMAP. Verify `uv run pytest -q` (no `--run-live-server`) is green and `.github/workflows/test.yml` is unchanged

## 2. live-acceptance

- [x] 2.1 Implement `tests/features/live/mailbox.py`: stdlib `imaplib` poll/parse/delete for subject `Tu código de BCRA Mini-RAG`, `\b(\d{6})\b` in body, subject must not contain the digits. IMAPS 993 when SSL true; plain IMAP when SSL false (not STARTTLS). New-mail path: Date/`INTERNALDATE` ≥ request (or click) time. Leftover path (HTTP fixture only): recent UNSEEN + subject within OTP TTL (~300s). After successful verify, mark/delete the used message. Unit-test parse, leftover reuse, TTL-bounded search, “used message must not be reused after verify”, and “no new message within bound” with a fake IMAP mailbox in `tests/test_live_mailbox.py` (no network). Verify `uv run pytest tests/test_live_mailbox.py -q`
- [x] 2.2 HTTP client helper: `httpx.Client` against `LIVE_BASE_URL`, send `Origin` and `Referer` matching that URL, cookie jar. Fail-fast if `LIVE_BASE_URL` does not origin-match `AUTH_PUBLIC_ORIGIN` (error names both URLs). Session-scoped fixture: `POST /auth/request` → new UID or leftover UNSEEN reuse → `POST /auth/verify` → mark/delete used mail; reuse cookies; do not mutate that client on logout (clone for logout; no extra SMTP). Unauthenticated `/chat/clear` always sends `{"session_id": …}`. Fail if `--run-live-server` and IMAP env missing. HTTP 429 from the process limiter is a fail-fast fixture error naming the limiter. Verify helper unit tests for Origin header construction and coalesce fallback with `uv run pytest tests/test_live_mailbox.py -q` (or a small `tests/test_live_http_client.py` with respx); default suite still has no live network
- [x] 2.3 Gherkin `tests/features/live/auth_http.feature` + steps (`@live_http` + `@live_server`): allowlisted IMAP OTP (body digits, not subject; leftover unused OTP counts as success); allowlist miss 200 and no new mail; verify → `/auth/me`; single-use after wait ≥ 2s; logout clone then `POST /chat` 401; unauthenticated `/auth/me` not 401; unauthenticated `POST /chat` 401; unauthenticated `POST /chat/clear` with a session id 401; `GET /health` 200 with dump fields. Unique Then phrases; do not copy `chat.feature` / `test_chat_bdd.py` step strings. Leave `tests/features/chat.feature` on fakes. Verify default `uv run pytest tests/features/test_chat_bdd.py -q` still green (live skipped, no duplicate-step error)
- [x] 2.4 Gherkin `tests/features/live/chat_http.feature` (`@live_http` + `@live_server`): authenticated structured `POST /chat`; no-advice / injection / weather → HTTP silencio + HTTP block; named A 3500 citation id with skip/fail matrix (skip `index_ready` false, skip silencio `llm_unavailable` or `missing_document`, fail other silencio including `cite-or-abstain`, require `A3500` when not silencio); A 9999 HTTP silencio and empty citations (skip only when `index_ready` false; do not skip on `missing_document`); authenticated clear has no citations. No `llm.calls` asserts. Unique Then phrases (`the HTTP finding is silencio`, `the HTTP no-advice rule is block`, `an HTTP citation id is "A3500"`, …). Verify default pytest still skips these and still collects
- [x] 2.5 Playwright fixture (Chromium, headless) + cookie inject from the HTTP session. Cookie-less context for logged-out scenarios. Gherkin `tests/features/live/auth_ui.feature` (`@live_ui` + `@live_server`): logged-out login row; Enviar shows `Tenés que ingresar con tu email.`; Clear after logout keeps turns (cloned context: cookie inject, UI weather seed, logout, Clear — HTTP `POST /chat` does not fill the chatbot); login-with-IMAP (wait once on 60s/Retry-After if HTTP already sent; poll only UID ≥ Enviar-código click; no leftover fallback); logout returns login row without wiping chat. Locators via `elem_id`. Verify `uv run pytest tests/test_ui.py -q` unchanged
- [x] 2.6 Add `elem_id`s `#observatory-input`, `#observatory-send`, `#observatory-clear` on the question box, Enviar, and Clear. Gherkin `tests/features/live/observatory_ui.feature` (`@live_ui` + `@live_server`): default Usuario; unauthenticated Staff hides chrome; authenticated Staff/Usuario toggle keeps conversation (seed weather via Enviar in that context once); four canned `CANNED_PROMPTS` strings including A 9999 (do not assert dump-answerability); Usuario in-corpus hides inspector/thinking (A 3500 skip matrix); Staff in-corpus shows inspector/trust and thought-group must not contain `Fuente:` if present (same skip matrix); Staff A 9999 abstain banner (skip only if `index_ready` false); viewports 1280 vs 375 after cookie inject and Vista Staff (IA). Sequential; fail-fast on HTTP 429. Verify default `uv run pytest -q` skips `live_server`
- [x] 2.7 Run `uv run pytest -q` then `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. Do not run a paid L1 eval. Do not run `--run-live-server` in CI
