## 1. authentication

- [x] 1.1 Create vertical package `src/bcra_rag/auth/` (`settings.py` `AuthSettings` with `env_prefix="AUTH_"`, `ports.py` `Mailer`, `fake.py` `FakeMailer`, `smtp.py` `SmtpMailer`, facade `build_auth` / `AuthModule` in `__init__.py`). Do **not** add a port under `bcra_rag.ports`. Root RAG `Settings` stays without AUTH fields. Verify `uv run pytest tests/test_auth.py tests/test_settings.py -q`: AuthSettings defaults match the spec; `bcra_rag.ports.__all__` is still the five RAG names; extra env ignored
- [x] 1.2 Implement `AuthService` in `src/bcra_rag/auth/service.py`: normalize email (`+tag` collapse), allowlist, HMAC-SHA256 OTP storage, `secrets.randbelow` 6-digit mint, 5 min TTL, replace-previous, 5-fail burn, `compare_digest`, signed cookie, limit buckets (5 emails/IP/day, 1 send/email/min, 10 send/email/day, 20 send/IP/day, 15 fail/IP/hour + 15 min cooldown, 1 verify/email/2s, 200 sends/process/day). Generic success on allowlist miss (no send). Logs: hash prefix + outcome, never OTP/email/cookie. Verify `uv run pytest tests/test_auth.py -q` covering those cases
- [x] 1.3 Auth `APIRouter` in `src/bcra_rag/auth/routes.py`: `POST /auth/request`, `POST /auth/verify` (Set-Cookie HttpOnly/SameSite=Lax/Max-Age=604800/Secure-if-https), `POST /auth/logout` (clear cookie), `GET /auth/me`. Origin check when `AUTH_PUBLIC_ORIGIN` is set. Missing/short secret → 503 on `/auth/*`. `mount_auth` + `build_auth` from `build_app` / `create_fastapi` (tests inject FakeMailer). Chat routes stay in `api/routes.py` and only call `email_from_request`. Verify `uv run pytest tests/test_auth_http.py -q`: send+verify cookie flags; allowlist miss 200 and no FakeMailer send; 429; origin reject; me unauthenticated 200; logout then me false

## 2. platform

- [x] 2.1 In `handle_turn`, call `bcra_rag.auth.email_from_request` **before** demo key, rate limit, retrieval, and LLM. 401 `authentication required`. Do not mint/write `SessionStore` on 401. Share `client_id_for(..., trusted_proxy=auth.settings.trust_proxy)` with auth limits. `DEMO_API_KEY` remains an extra gate after the cookie. Verify `uv run pytest tests/test_chat_api.py tests/test_handle.py -q`: no cookie → 401 and `llm.calls == []`; demo key without cookie → 401; cookie then named A 3500 still cites; `/health` 200 without cookie; unauthenticated 401s do not consume the chat rate-limit budget
- [x] 2.2 Update `make_client` / chat fixtures / `tests/features/test_chat_bdd.py` so in-corpus Gherkin authenticates (FakeMailer + verify, or a signed test cookie). Add a Gherkin (or HTTP) scenario: unauthenticated chat → 401 and the language model is not called. Verify `uv run pytest tests/features/test_chat_bdd.py tests/test_chat_api.py -q`

## 3. assistant-ui

- [x] 3.1 Default Blocks tree to Usuario: Vista value `LAYOUT_USER`, freeze/side `visible=False`, shell `layout-user`. Change `apply_layout` to take authentication: Staff chrome only when authenticated. Add `#auth-login` row (Spanish copy pinned in design) visible while logged out; logout + email while logged in. `demo.load` hydrates from cookie without auto-switching to Staff. CSS for `#auth-login`. Verify `uv run pytest tests/test_ui.py -q`: default freeze/side hidden; login elem_id present; `apply_layout(LAYOUT_STAFF, authenticated=False)` hides; `authenticated=True` shows; LAYOUT_HELP still names both layouts; existing observatory contracts remain
- [x] 3.2 Gradio login buttons `fetch` `/auth/*` with credentials; Python follow-up reads `gr.Request` cookies. `_turn` / `_clear` go through `handle_turn` (401 Spanish `Tenés que ingresar con tu email.`, no LLM). Pass `on_thinking` and fill inspector/trust only when authenticated **and** Vista is Staff. Logout forces Usuario and shows login row; does not `/clear` chat. Verify `uv run pytest tests/test_ui.py -q`: unauthenticated turn does not call the LLM; staff outputs empty in Usuario; thought path unchanged for authenticated Staff

## 4. docs

- [x] 4.1 Document `AUTH_*` in `.env.example` (create if missing), `deploy/env.remote.example`, and README `## How to run` (copy-env paragraph: secret ≥32, allowlist, SMTP, `/chat` needs a session from `/auth/verify`; `AUTH_TRUST_PROXY` only behind a proxy that overwrites XFF; auth is the `bcra_rag.auth` module). No new command fence unless a command is added. Verify README names those vars and `uv run pytest tests/test_settings.py tests/test_auth.py -q`
- [x] 4.2 Run `uv run pytest -q` then `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. Do not run a paid L1 eval
