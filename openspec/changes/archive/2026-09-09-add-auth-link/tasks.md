## 1. authentication — session lifetime and OTP extractors

- [x] 1.1 Change `AuthSettings.session_days` default to 1 (Max-Age 86400). Update `tests/test_auth.py` (`test_auth_settings_defaults`, rename `test_correct_code_authenticates_for_a_week` to 24h: still valid at 24h−10s, `None` after +20s) and `tests/test_auth_http.py` (`max-age=86400`). Verify `uv run pytest tests/test_auth.py tests/test_auth_http.py -q`
- [x] 1.2 Replace `\b(\d{6})\b` OTP parsers with labeled `Tu código de acceso es (\d{6})` in `tests/test_auth.py`, `tests/test_auth_http.py`, `tests/chat_fixtures.py` (`login_client`), and `tests/features/live/mailbox.py`. Prefer one shared helper. Verify existing OTP tests still pass: `uv run pytest tests/test_auth.py tests/test_auth_http.py tests/test_chat_api.py -q`

## 2. authentication — multipart mail

- [x] 2.1 Add `otp_html_body` next to `otp_body` in `mail_copy.py` (table, inline CSS + `bgcolor`, observatory hex, `Iniciar sesión`, visible URL, no mailbox, no `<img>`, `html.escape`). Verify `uv run pytest tests/test_auth.py -q`: helper with `&`/`"` in the URL is escaped; `lang="es"`; `rel="noopener noreferrer"`; hex `#04111d` / `#72d6cb` / `#03101c`; both parts contain the 6-digit code; subject has neither code nor token; no mailbox in HTML
- [x] 2.2 `send_otp` takes optional `login_url`. `SmtpMailer` `set_content` + `add_alternative`. `FakeMailer` records `html`. Unset public origin omits the URL from both parts. Verify `uv run pytest tests/test_auth.py -q`: allowlisted send has HTML + plain; unset origin has code and no login URL in either part

## 3. authentication — login token and hybrid consume

- [x] 3.1 `AuthService.request_otp` returns `intent_nonce` and optional `login_url` (no HTTP). When `public_origin` is set mint `secrets.token_urlsafe(32)`, store raw intent nonce + `link_digest` on the `_Otp` slot (5 min). Join URL as `public_origin.rstrip("/") + "/auth/link/" + token`. Coalesce: do not mint a new nonce or token; return the stored nonce. `POST /auth/request` in `routes.py` sets `auth_intent` (`HttpOnly`, `SameSite=Lax`, `Path=/auth/link`, `Max-Age=otp_ttl_s`, `Secure` via `cookie_secure`). Verify `uv run pytest tests/test_auth.py -q`: token hashed not stored raw; URL has no email, no 6 digits, and no double slash when origin has a trailing `/`; coalesce leaves original token valid and the same nonce; allowlist miss sends nothing
- [x] 3.2 Routes: `GET /auth/link/{token}` washes only (Set-Cookie `auth_link`, 302 `/auth/link`, never `session`; `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, `X-Frame-Options: DENY`, CSP `frame-ancestors 'none'`). HEAD does not consume. **Do not** call `_reject_bad_origin` on GET. Tests use `build_app` (Gradio mounted), `follow_redirects=False`. Verify `uv run pytest tests/test_auth_http.py -q`: 302 Location has no token; no session cookie; headers present; GET with Referer `https://mail.google.com` is not 403; OTP still verifies after a scanner GET; response is not Gradio HTML
- [x] 3.3 Washed `GET /auth/link` auto-consumes only with matching `auth_intent` + `Sec-Fetch-Mode: navigate` + `Sec-Fetch-Dest: document` (session 24h, burn OTP+token, 302 `/`). Missing Sec-Fetch or missing intent → confirm HTML (`lang=es`, mailbox in body and button, native POST form, token not in HTML). `POST /auth/link` origin-checks; 403 does not burn. Failed consumes share verify IP lockout and do not burn another mailbox's OTP. After a coalesced `/auth/request`, same-browser auto-login still works. Verify `uv run pytest tests/test_auth.py tests/test_auth_http.py -q`: auto-login happy path; scanner/default TestClient does not sign in; two clients (login CSRF) no auto-login; confirm POST signs in and burns OTP; origin mismatch 403 token still live; unknown-token POSTs do not burn `ops@`/`other@`; used/expired/unknown generic Spanish failure linking to `/` without a token; concurrent consume at most one session; logs omit token/OTP/email/`Set-Cookie`
- [x] 3.4 Intent/link cookies `Path=/auth/link` are not sent to `/chat` (still 401 until verify/link). Logout after link login still clears session. HTTPS public origin marks `auth_intent`, `auth_link`, and `session` Secure; local HTTP omits Secure. Verify `uv run pytest tests/test_auth_http.py tests/test_chat_api.py -q`

## 4. platform — daily language-model caps

- [x] 4.1 Add `CHAT_TURNS_PER_EMAIL_DAY` (30) and `CHAT_TURNS_PER_PROCESS_DAY` (100) to RAG `Settings`. In `handle_turn`, after session + demo key, skip `/clear` (do not increment; burst limiter may still see clear), then email cap then process cap then the existing 20/60s limiter. Increment when the question is accepted past session/demo; a cap 429 does not increment. 429 logs `email_cap` / `process_cap` plus email hash prefix, never raw email. Verify `uv run pytest tests/test_chat_api.py tests/test_settings.py -q` with shrunk caps (email 2 / process 3): third chat 429 and `llm.calls == 2`; process cap across two mailboxes; plus-tag shares bucket; `/chat/clear` does not increment; unauthenticated 401s do not consume caps; UTC day rollover resets; burst limiter still works when caps are high; caplog has no raw email

## 5. assistant-ui — confirm interstitial and logout

- [x] 5.1 Confirm page Spanish, one `h1`, mailbox named, `<form method="post" action="/auth/link">` works without script. After 302 `/`, existing `demo.load` still shows `#auth-logout`. Keep typed OTP row. Observatory `_turn` on HTTP 429 shows Spanish `Demasiados intentos. Probá más tarde.` (same as auth JS). Optional: Gradio `autocomplete` on Correo / Código if it needs no hack. Verify `uv run pytest tests/test_ui.py tests/test_auth_http.py -q`: `#auth-logout` still in the Blocks tree; login JS still posts `/auth/request` and `/auth/verify`; confirm HTML has form/h1/`lang=es` and no token; 429 chat path uses the Spanish notice; no Gradio SSR dependency on `GET /`

## 6. docs and coverage

- [x] 6.1 Document `AUTH_SESSION_DAYS=1`, mail URL from `AUTH_PUBLIC_ORIGIN`, `CHAT_TURNS_PER_EMAIL_DAY`, `CHAT_TURNS_PER_PROCESS_DAY` in README `## How to run` copy-env paragraph, `.env.example`, and `deploy/env.remote.example`. No new command fence. Verify `uv run pytest tests/test_settings.py tests/test_notes.py -q` (extend `test_readme_how_to_run_names_auth_vars` if those names are in the copy-env paragraph)
- [x] 6.2 Run `uv run pytest -q` then `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. Do not run a paid L1 eval. Do not spawn uvicorn from tests
