## 1. query-answering

- [x] 1.1 Add `parse_llm_draft` in `src/bcra_rag/adapters/llm_openai.py` that JSON-loads a completions body, coerces `citations` (string, list of strings, objects missing `tipo`) into dump-id citation objects (`texto_ordenado` → `TO`, `A` + digits → `A`), treats missing/unusable citations as `[]`, and maps invalid `finding` to `silencio`. Verify `uv run pytest tests/test_llm_port.py -q` (new cases): `Fuente: texto_ordenado` yields id `texto_ordenado` tipo `TO`; `["A8359"]` yields tipo `A`; object `{id: texto_ordenado}` without `tipo` yields `TO`; unusable citations yield `[]`; invalid finding is `silencio`
- [x] 1.2 Call `parse_llm_draft` from `LlmAdapter.complete` instead of raw `model_validate_json`; stub the OpenAI client (no network). Tighten system text in the adapter and `_prompt` in `answer_query.py` so `citations` is an array of `{id, tipo, punto, snippet}` and `Fuente:` belongs in `answer`; keep `response_format` `json_object`. Verify `uv run pytest tests/test_llm_port.py -q`: a stub completions payload with `citations` as a string returns a valid `LlmDraft` and does not raise
- [x] 1.3 Keep `AnswerQuery` merge-from-hits unchanged. Add a use-case test that a valid draft with empty `citations` still returns dump citation objects; confirm `test_llm_failure_is_silencio_not_exception_text` still passes (`UnavailableLlm` → `silencio` / `llm_unavailable`, no exception text). Verify `uv run pytest tests/test_answer_query.py tests/test_llm_port.py -q`
- [x] 1.4 Run `uv run pytest -q` and `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml` and fix until green with src coverage >= 80%. No README command change
