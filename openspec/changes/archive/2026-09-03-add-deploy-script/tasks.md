## 1. host-deploy

- [x] 1.1 Add `deploy/bcra-rag.service`, `deploy/bcra-rag-ingest.service`, and `deploy/bcra-rag-refresh.service` (all three: `WorkingDirectory=` the install dir; API: `User=` the service user, `EnvironmentFile=` that dir’s `.env`, ExecStart absolute venv `uvicorn bcra_rag.api.app:app --host 127.0.0.1 --port 8000`, no `--workers`, Restart=always, `[Install] WantedBy=multi-user.target`; oneshots: Type=oneshot, root, TimeoutStartSec=infinity, ExecStart the helper scripts, no `[Install]`) and verify unit tests lock those invariants and that **none** of the three units contain `Conflicts=`
- [x] 1.2 Add `deploy/ingest.sh` and `deploy/refresh.sh` (`flock /run/bcra-rag-job.lock`, trap `systemctl start bcra-rag` on EXIT, stop API, `cd` to the install dir, `sudo -u` the service user and the absolute `.venv/bin/python -m bcra_rag.jobs.ingest` / `jobs.refresh`) and verify tests for flock, trap/stop/job order, `cd`, and absolute venv python module paths
- [x] 1.3 Add `deploy/bcra-rag-refresh.cron` (`0 6 * * * root systemctl start bcra-rag-refresh.service`) and verify a unit test that the line has username root, daily 06:00, and starts the refresh unit (not a timer)
- [x] 1.4 Add `scripts/deploy.sh` (requires `DEPLOY_HOST`; optional `DEPLOY_USER` / `DEPLOY_DIR`; rsync excludes `.git/`, `.venv/`, `data/`, `__pycache__/`, `.env` and does **not** pass `--delete-excluded`; bootstrap poppler and uv; every remote uv call is `$HOME/.local/bin/uv`; `uv sync --frozen --no-dev`; install the three units and `/etc/cron.d/bcra-rag-refresh`; `daemon-reload`, `enable`, **`restart`** API; seed missing `.env` from `deploy/env.remote.example` and never overwrite an existing `.env`; exit non-zero if `LLM_API_KEY` or `EMBEDDING_API_KEY` empty; `--ingest` waits on `systemctl start bcra-rag-ingest`) and verify tests for host required, excludes, no `--delete-excluded`, `$HOME/.local/bin/uv`, `restart`, seed-once, key fail-closed, and `--ingest`
- [x] 1.5 Add `deploy/env.remote.example` with `EMBEDDING_BASE_URL` pointing at a loopback OpenAI-compatible embedding server, `EMBEDDING_MODEL=qwen3-embedding-0.6b`, dummy `EMBEDDING_API_KEY=sk-local`, xAI `LLM_BASE_URL`/`LLM_MODEL`, empty `LLM_API_KEY`, `DATA_DIR=data`, no `export` lines; leave root `.env.example` for local; verify a test that the remote seed has those embedding values and that Settings still ignores extra env keys
- [x] 1.6 Update README.md `## How to run` with one brief line each for first deploy, fill-keys-and-redeploy, update (`./scripts/deploy.sh`), SSH local-forward to 127.0.0.1:8000, `--ingest`, `systemctl` start/stop/status/ingest/refresh, and that the embedding server at `EMBEDDING_BASE_URL` must be up; keep existing Docker bullets; extend `tests/test_notes.py`; verify those strings are present
- [x] 1.7 Run `uv run pytest -q` and `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml` and fix until green with src coverage >= 80%
