## 1. ingest-logging

- [x] 1.1 Switch `configure_logging` to stdlib `LoggerFactory` + `ProcessorFormatter` JSON, always attach stdout, and optionally append a `FileHandler` when `log_file` is passed (`force=True`; create parent dirs). Verify `uv run pytest tests/test_logconfig.py -q` (new): a log line is JSON on stdout and in the temp file with `event`, `level`, and `timestamp`; omitting `log_file` does not create a file
- [x] 1.2 After `build_ingest()`, `jobs.ingest` and `jobs.refresh` call `configure_logging(log_file=settings.data_dir / "logs" / "ingest.log")`. Verify a unit test (tmp `DATA_DIR`, fake catalog/index as needed) that running the job module creates that file and that `app.py` still calls `configure_logging()` with no file
- [x] 1.3 In `IngestCorpus.run`, after the mode-filtered catalog list, log `ingest_run_started` with `mode` and `total = len(documents) + 1`; wrap `_ingest_to` and each `_ingest_comunicacion` with `ingest_document_started` / `ingest_document_finished` (`doc_id`, `name`, `fecha`, `processed`, `total`) so skips still increment `processed`. Verify `uv run pytest tests/test_ingest.py -q`: empty dump of three catalog ids logs `total=4` before any upsert; A13 lines include `A13`, CAMEX-1, `1981-03-02`; TO lines include `texto_ordenado` and Exterior y Cambios; a second full run that skips unchanged ids still reaches `processed=total`
- [x] 1.4 Update README.md `## How to run` Debug so job logs are JSON on stdout **and** appended to `DATA_DIR/logs/ingest.log` (default `data/logs/ingest.log`). Verify `uv run pytest tests/test_notes.py::test_readme_operator_bullets -q` after extending that test for the log file path
- [x] 1.5 Run `uv run pytest -q` and `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml` and fix until green with src coverage >= 80%
