## 1. web-analytics

- [x] 1.1 Idempotent Matomo site: search the public observatory origin; if missing, `SitesManager.addSite` (timezone `America/Argentina/Buenos_Aires`, `excludeUnknownUrls` on, no loopback aliases). Record `idSite`. Verify MCP `matomo_site_get` returns that origin.
- [x] 1.2 Add `matomo_url` and `matomo_site_id` to chat `Settings` (empty default). Implement `matomo_snippet` + `observatory_head(...)` (cookieless, DNT, heartbeat 15, loopback skip unless `?matomo=1`, fail-open sanitize). Pass settings through `mount_ui` / `create_fastapi`. Do not inject via `_RewriteGradioHtml`. Do not touch mail-link pages. Verify `uv run pytest tests/test_ui.py tests/test_settings.py -q`.
- [x] 1.3 Unit tests: unset/blank omits `_paq`; valid `https://tracker.example/matomo/` + `42` includes site id, `disableCookies`, `setDoNotTrack`, `matomo.php`, loopback names, `matomo=1`, no mailbox/question text; http/quotes/userinfo/non-digit omit snippet; `mount_ui` without settings equals `observatory_head()`; with settings matches those values. Verify the same pytest command.
- [x] 1.4 Document empty `MATOMO_URL` / `MATOMO_SITE_ID` in `.env.example`, `deploy/env.remote.example`, and README `## How to run` (Debug / optional collectors). Unset sends nothing; URL is an https tracker origin ending in `/`; loopback skipped. Do not name the public hostname. No new command. Verify `uv run pytest tests/test_notes.py tests/test_no_private_layout.py tests/test_settings.py -q`.
- [ ] 1.5 Append the two keys to gitignored laptop `.env` and dump-host `.env`; `systemctl restart bcra-rag`. Verify a real browser on the public origin loads `matomo.js` and MCP `VisitsSummary.get` for that `idSite` sees a visit (curl of `GET /` only proves the snippet is in `gradio_config.head`).
- [x] 1.6 Run `uv run ruff check .`, `uv run mypy src`, then `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. Do not run a paid L1 eval. Do not run `--run-live-server` or `--run-prod-smoke` as a CI-blocking task.
