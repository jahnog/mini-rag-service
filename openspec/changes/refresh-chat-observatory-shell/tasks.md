## 1. assistant-ui

- [x] 1.1 Add `src/bcra_rag/ui/observatory.css` (tokens: `#04111d`, `#72d6cb`, `#ffd27d`, glass `28px`, grid overlay, `color-scheme: dark`) and `src/bcra_rag/ui/theme.py` with `observatory_theme`, `observatory_css_path`, `observatory_head`. Wire them on `mount_ui` via `gr.mount_gradio_app` (`css_paths`, `theme`, `head`, `footer_links=[]`, `run_history=False`). Hatch force-include the CSS. Verify `uv run pytest tests/test_ui.py -q`: CSS contains `#04111d`, `#72d6cb`, `28px`; helpers return that CSS path and a `theme-color` of `#04111d`; `mount_ui` passes `css_paths`, `theme`, `head`, `footer_links=[]`
- [x] 1.2 Restructure Blocks to `#observatory-shell` / `#observatory-topbar` / `#observatory-layout` / `#observatory-stage` / `#observatory-side` / `#observatory-footer`; Enviar primary and Clear secondary; four canned-prompt Buttons (not `gr.Examples`); L1 accordion still `open=False`. Add `topbar_markdown` and keep `banner_markdown` freeze fields. Verify `uv run pytest tests/test_ui.py -q`: those `elem_id`s exist on the Blocks tree; `banner_markdown`/`topbar_markdown` still contain `to_as_of`, `last_refresh`, last A, n_docs, and “no oficial”; `CANNED_PROMPTS` length 4 with A 3500/A 8359, liquidar/export, 2001–2002, A 9999; `L1_ACCORDION_OPEN_DEFAULT is False`; `build_blocks` still does not call the LLM or L1
- [x] 1.3 Side inspector: citation Radio + `#citation-card` markdown as the visible surface; copy-id `Textbox(buttons=["copy"], interactive=False)`; `trust_markdown` chips on `#trust-panel`; `gr.JSON` inspector/trust `visible=False`; abstain markdown stays in the stage. Verify `uv run pytest tests/test_ui.py -q`: existing inspector/trust/abstain assertions; card markdown still has id, snippet, `bcra.gob.ar`, copy-id; trust markdown includes rule names and pass/warn/block; silencio still sets abstain visible
- [x] 1.4 Run `uv run pytest tests/test_ui.py -q` then `uv run pytest -q` and `uv run pytest -q --cov=src --cov-report=term-missing --cov-report=xml`. Fix until green with src coverage >= 80%. No README `## How to run` edit (no command added)
