HOLE_NOTE = """# BCRA CAMEX dump notes

This dump contains official CAMEX Comunicaciones "A" plus the Exterior y Cambios
texto ordenado. It does not crawl untagged A-series ids solely to fill the
1990–97 CAMEX sequence hole (tags jump from 232 to 314).

Wipe and rebuild by deleting the dump directory (`data/` by default),
then run one-time ingest again.
"""
